# CrewAI Flows - YAML Configuration Examples

YAML configuration patterns for `crewai-flows`.

## YAML Guidance

This skill does not currently include native YAML snippets in source material.
Use this template when integrating with CrewAI project configs:

```yaml
name: example
description: Replace with skill-specific configuration
```
