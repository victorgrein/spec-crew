# Flows Examples

Example focus areas for the `flows` pack:

- Single-path flow setup
- Router-based branching flows
- Parallel branch execution and merge conditions
- Crew integration from flow stages
